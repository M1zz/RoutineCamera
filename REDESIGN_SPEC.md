# 세끼 리디자인 스펙 — "얼마나 먹었나" (진행 중)

식단 기록 이탈 심리 리서치에서 출발해, 메인 UX를 격자(3끼 슬롯)에서
"순간 컬렉션 + 앱 안 여는 원탭 기록"으로 전환하는 설계.

## 1. 문제 (리서치 근거)

식단 기록 이탈의 3대 심리:
- **완벽주의/올오어낫씽** — 100% 못 채울 바엔 안 함 + 추적 피로
- **나쁜 날 회피** — 많이 먹은 날일수록 기록을 피함(=증거 남기기 싫음)
- **추적 피로** — 칼로리·양·끼니 세부 입력의 누적 부담

근본 원인: **격자 UI가 "채워야 할 빈 슬롯"을 미리 그려서(뺄셈식) 비어있으면
자동으로 "실패"로 읽힘.** 색을 아무리 순화해도 빈 칸이 존재하는 한 압박은 남음.

## 2. 방향 전환

- 정체성: "기록 앱" → **"음식 사진 일기 / 순간 컬렉션"**
- 모델: **의무(채우기) → 수집(쌓기)** (덧셈식 — 없는 건 안 보이니 채울 것도 없음)
- 핵심 지표: 칼로리 ❌ → **"얼마나 먹었나"(양)** — 식전/식후 사진의 차이로 포착

## 3. 핵심 UX 3가지

1. **앱 안 열고 원탭 기록** — 잠금화면/홈 위젯 버튼, 액션버튼, 단축어/시리
   → 앱을 안 여니 "빈 칸의 벽"을 마주칠 일이 없음 (완벽주의 트리거 소멸)
2. **슬롯 없는 순간 컬렉션 피드** — 아침/점심/저녁 슬롯 폐기, 남긴 순간만 시간순으로 쌓임
   (끼니타입은 없애지 않고 촬영 시각으로 자동 태깅 → 통계/친구/운동모드 유지)
3. **"다 먹음" 한 탭 = 식후 신호** — 식후 사진의 대체. 위젯 원탭으로 앱 없이.

## 4. 기록 모델 (식전 / 식후 / 다 먹음)

기존 데이터 모델의 `beforeImageData`/`afterImageData` 슬롯 재사용 + `ateAll` 플래그 신설.
**셋 다 강제 없음** — 어느 조합이든 "완성"으로 인정(완벽주의 재발 방지).

| 식전 | 식후 | 다 먹음 | 해석 = 얼마나 먹었나 | 화면 인지 |
|:--:|:--:|:--:|---|---|
| ✓ | ✓ | – | before−after 차이 = 남긴 양 | 슬라이더/플립 비교 |
| ✗ | ✓ | – | **조절러**: 먹은 양 그 자체 | 사진 1장 (완성) |
| ✓ | ✗ | ✓ | **비조절러**: 덜어놓은 만큼 다 먹음 | 식전 사진 + "다 먹음" 뱃지 |
| ✓ | ✗ | ✗ | 아직 먹는 중 | 조용한 "식사 중" 상태 |
| ✗ | ✗ | ✓ | 사진 없이 다 먹음만 | 최소 기록 |

## 5. 두 페르소나

- **밥양 조절러**: 적당히 덜어 먹으니 **사진 한 장이면 끝**. 비교 불필요.
- **밥양 비조절러**: **식전 사진만으로 자기 객관화**("이만큼이나 담았네") →
  먹고 나서 **"다 먹음" 한 탭**. "식전+다먹음"이 쌓이면 피드에서 과식 패턴이 스스로 보임.
  숫자·칼로리 없이 자기 객관화.

## 6. "다 먹음" 신호 흐름

식전만 찍은 경우 → 일정 시간 뒤 **은근한 식후 알림** → **위젯/잠금화면에서 "다 먹음" 원탭**
(앱 안 열고). 원탭 컨셉과 직결.

## 7. 구현 단계

### Phase 1 — 앱 본체 (새 타깃 불필요, 시뮬레이터 검증 가능)
- [ ] `MealRecord`에 `capturedAt: Date?`(타임스탬프) + `ateAll: Bool` 추가 (기존 데이터 호환 디코딩)
- [ ] "다 먹음" 기록 경로(store) + 식전/식후/다먹음 조합 상태 판정 헬퍼
- [ ] **MomentsView**: 슬롯 없는 시간순 사진 피드 (오늘/어제/날짜 그룹, 끼니 자동태그는 옅은 라벨)
- [ ] 단일 "+ 기록" 진입 (끼니 자동 판별) + 식전/식후 버튼
- [ ] 식후 알림(식전 찍힌 뒤 일정 시간) — 앱 없이 "다 먹음" 유도
- [ ] `AppIntent`("다 먹음"/기록) → 액션버튼·단축어·시리 (앱 본체 타깃)
- [ ] 기존 격자는 삭제하지 않고 설정 토글 뒤에 보존 (되돌리기 안전장치)

### Phase 2 — Widget 익스텐션 (새 Xcode 타깃 + App Group)
- [ ] Widget Extension 타깃 생성, App Group으로 데이터 공유
- [ ] 잠금화면(accessoryCircular/Rectangular) + 홈 위젯 인터랙티브 버튼 → AppIntent로 원탭 기록

## 원칙 체크리스트 (모든 화면에 적용)
- 빈 칸/미기록을 시각적으로 "실패"로 표시하지 않는다
- 칼로리·정밀 수치·완료율 % 없음
- 어떤 최소 기록도 "완성"으로 인정
- 압박(스트릭 리셋·빨강·나그성 알림)보다 회복·수집·자기객관화
