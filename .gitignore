# Swift Package build artifacts
.build/
RoutineCameraCore/.build/

# macOS
.DS_Store

# Xcode / SwiftPM 사용자 상태
RoutineCameraCore/.swiftpm/

# Xcode 사용자별 상태 (스킴 순서 등) — 사람마다 달라 커밋할 이유가 없다
xcuserdata/

# DeployBar 아카이브·내보내기 산출물 — 배포할 때마다 새로 만들어진다
build/
