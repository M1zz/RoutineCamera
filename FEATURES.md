# 세끼 (RoutineCamera) 기능 목록

## 1. 기록 (핵심)
- **순간 컬렉션 피드** (`MomentsView`): 슬롯 없이 남긴 기록만 시간순으로 쌓이는 사진 일기. 빈 칸 없음.
  - 일자 그룹(오늘/어제/날짜), 같은 날 시간순 정렬(아침 아래 → 저녁 위)
  - List 기반 → **스와이프 삭제**, 탭 → **상세 push**(네비게이션 스택)
  - 기록 버튼이 "무엇으로 기록될지" 미리 표시 (예: "점심 기록하기")
- **격자 뷰** (`DailySectionView`, 레거시): 아침·점심·저녁 칸. 설정 토글로 전환.
- **끼니 자동 추론** (`MealType.inferred`): 촬영 시각 기준 (5~10 아침 / 11~15 점심 / 16~21 저녁 / 그 외 간식)
- **기록 방식**: 식전/식후 사진, 사진 없이 기록, **"다 먹음"** 신호, 메모
- **식단/운동 모드**: 완전히 별도 저장. 헤더 버튼 + 설정 토글로 전환. 운동은 운동 전용 라벨/경험.

## 2. 동기부여 (완벽주의 완화 설계)
- **스트릭**: "최소 한 끼라도" 기록하면 이어짐 (`isDayRecorded`)
- **누적 기록일**: 절대 줄지 않는 카운터 (`getTotalRecordedDays`)
- **목표**: 누적 기록일 기준(놓쳐도 안 줄어듦)
- **통계**: 주/월 "N일·M끼 기록" 긍정 카운트(달성률% 없음), 식사별 기록
- **업적** (`AchievementManager`): 연속/식사별/총 기록 수 기반

## 3. 알림 · 앱 안 여는 기록
- **챙길 식사**: 고른 식사만 리마인드 (첫 실행 프롬프트 + 설정)
- **식사 리마인드**: 식사 시간 + 2시간 후, 식사 시간 커스텀
- **기록 시간 배너**: 홈 상단
- **"다 먹음" 알림 액션**: 식전만 찍으면 알림 → 앱 안 열고 원탭
- **App Intents** (`LogAteAllIntent`): 액션버튼 / 시리 / 단축어로 "다 먹음"
- **위젯 배관**: App Group 공유 저장소 + 대기열(`drainPendingAteAll`) (위젯 타깃은 별도)

## 4. 소셜 (CloudKit)
- **친구** (`FriendManager`): 친구 코드로 추가, iCloud 계정 신원
- **식단 공유**: CKAsset로 사진 업로드 (RoutineCamera 컨테이너)
- **친구 커멘트·콕찌르기**: 친구 기록에 피드백 + 푸시
- **개발자 피드백** (LeeoKit): FeedbackHub 컨테이너

## 5. AI 음식 분석
- **무료 분석**: Vision Framework (`VisionAnalyzer`)
- **고급 분석**: OpenAI Vision (`OpenAIFoodAnalyzer`) — 코인/구독(`CoinManager`/`SubscriptionManager`)

## 6. 기타
- **사진앱 자동 저장** (앨범 분리)
- **접근성**: VoiceOver 라벨/상태/커스텀 로터
- **만족도·리뷰** (LeeoKit): 참여도 기반 2갈래(리뷰/피드백)
- **데이터**: UserDefaults(App Group 공유) 로컬 저장, 기존 데이터 마이그레이션
