# 릴리스 노트

## 1.0.8 (2026.09.03)

### 앱스토어 "새로운 기능" (짧은 버전)

```
친구 화면에서 음식과 운동을 나눠서 봐요
친구 기록에 주고받은 응원이 모두 보여요
이모지를 눌러 간편하게 응원할 수 있어요
친구가 응원을 남기면 알림이 옵니다
그룹에서 오늘 기록한 사람이 한눈에 보여요
```

### 상세 변경 내역

**친구 화면: 음식 / 운동 (신규)**
- 상단 탭이 "타임라인 / 그리드"(보기 방식)에서 "음식 / 운동"(무엇을 보는지)으로 바뀌었습니다
- 지금까지 운동 기록은 친구에게 아예 올라가지 않았습니다. 이제 운동도 공유되고 따로 볼 수 있습니다
- 같은 날 같은 끼니라도 음식 응원과 운동 응원은 서로 섞이지 않습니다
- 그룹의 "오늘 기록함" 판정은 음식과 운동을 합쳐서 셉니다.
  운동만 하는 사람이 미기록으로 보이던 문제가 없어집니다
- 그리드 보기는 없앴습니다. 그리드의 빈 칸으로 하던 콕 찌르기는
  타임라인 맨 위의 "오늘 아직 기록이 없어요" 줄로 옮겼습니다
- 운동 **이력**은 자동으로 올라가지 않습니다. 업데이트 이후의 기록부터 공유됩니다

**응원을 대화처럼 (신규)**
- 친구 식단에 남긴 응원이 그 자리에 목록으로 남습니다. 예전에는 쓰기만 되고 다시 볼 수 없었습니다
- 내가 남긴 글은 오른쪽 초록으로, 다른 사람 글은 왼쪽으로 구분됩니다
- 보낸 뒤에도 화면이 닫히지 않습니다 — 방금 남긴 글이 목록에 뜨는 것을 그 자리에서 확인합니다
- 내 식단 상세의 "내가 보낸 피드백"은 그날 그 끼니에 **남에게** 보낸 글까지 딸려 나오고 있었습니다
  (이 식사와 무관한 글). 그 끼니의 대화만 보이도록 고쳤습니다

**이모지 반응 (신규)**
- 응원 화면에 이모지 줄이 생겼습니다. 누르면 바로 전송됩니다 (👍 🔥 😍 👏 🤤 💪 🎉 🥲)
- 같은 이모지가 여러 개면 하나로 묶여 개수와 함께 보입니다 (👍 2)
- 이미 남긴 이모지를 다시 누르면 취소됩니다
- 아직 업데이트하지 않은 친구의 앱에서는 짧은 메시지로 읽혀 깨지지 않습니다

**친구 응원 알림 (수정)**
- 친구가 응원을 남겨도 알림이 오지 않던 문제를 고쳤습니다
- 원인은 알림 등록이 실패해도 성공한 것으로 기록해 두고 다시 시도하지 않던 것이었습니다
- 설정 → 알림에 "친구 응원 알림" 상태가 표시되고, 꺼져 있으면 직접 다시 등록할 수 있습니다

**그룹 구성원 그리드**
- 멤버 전원이 타일로 깔리고, 그날 기록한 사람은 초록, 안 한 사람은 회색으로 보입니다
- 색만으로 구분하지 않도록 체크·빼기 기호를 함께 표시합니다
- 사진까지 공개하는 그룹에서도 맨 위에 같은 그리드가 뜹니다.
  예전에는 기록이 있는 멤버만 보여서 안 올린 사람이 화면에서 사라졌습니다
- 기록한 사람이 앞, 그 안에서는 이름순
- 예전 목록의 끼니 이름 칩(아침·점심·저녁)은 타일에서 "N끼"로 줄었습니다.
  음성 안내에는 끼니 이름이 그대로 들어갑니다

### 배포 준비 (사용자에게 보이지 않음)

- 버전을 `Config/Version.xcconfig` 한 곳에서 정하도록 통일 (앱·위젯 두 타겟이 어긋나던 위험 제거)
- `deploy.env`, `scripts/predeploy.sh` 추가 — 배포 전에 테스트와 Release 빌드를 통과해야 아카이브를 만든다

## 1.0.7 (2026.09.02)

### 앱스토어 "새로운 기능" (짧은 버전)

```
친구 화면과 기록 방식을 다듬었어요.

설정에서 식후 사진 남기기를 끄면 식전 사진 한 장으로 기록이 끝나요
식후 촬영 안내와 다 드셨어요 알림도 받지 않습니다
친구 화면을 열면 친구에게 보이는 내 이름이 맨 위에 바로 보여요. 이름은 계정 설정에서 언제든 바꿀 수 있습니다
식사 시간이 다가오면 피드에 자리가 미리 잡혀요. 기록하면 그 자리에 채워지고, 안 하면 조용히 사라집니다
친구 기록의 사진이 잘리지 않고 원본 비율 그대로 보여요
친구가 몇 시에 먹었는지 실제 기록한 시각이 보입니다
친구 타임라인에서도 바로 응원을 남길 수 있어요. 예전에는 그리드로 바꿔야만 보였습니다
기록이 없는 날에도 콕 찌르기와 응원을 남길 수 있어요
운동을 저녁에 해도 저녁 자리에 기록됩니다. 하루에 여러 번 기록할 수도 있어요
운동 기록에 아침, 점심 같은 끼니 이름 대신 운동이라고 표시돼요
사진을 한 장만 남겼을 때 무엇이 비었는지 알려줍니다. 식전 사진 기록 필요, 식후 사진 기록 필요
그룹 만들기와 참여 화면에서 키보드가 내려가지 않던 문제를 고쳤어요
저장에 실패했을 때 알 수 없는 영어 오류 대신 무엇이 문제인지 한글로 알려드려요
```

### 상세 변경 내역

**식후 사진 남기기 옵션 (신규)**
- 설정 → 화면에 "식후 사진 남기기" 토글 추가 (식단 모드 전용, 기본 켜짐 — 기존 사용자는 동작 변화 없음)
- 끄면 카메라의 식전/식후 선택이 사라지고 항상 한 장만 저장
- "다 드셨어요?" 알림을 예약하지 않고, 끄는 즉시 이미 예약된 알림도 모두 취소
- 사진 위 빨간 "1" 뱃지(= 식후 사진이 아직 없다는 표시) 숨김
- 상세 화면에서 식후 페이지, "식전/식후" 라벨, "식전/식후 사진 알림 가리기" 토글 제거
- 순간 피드에서 "식후 사진 기록 필요" 대신 "기록 완료"로 표시 — 사진 한 장이 곧 마감이므로 미마감 상태 자체가 없음

**친구에게 보이는 이름**
- 친구 화면 맨 위에 "친구에게 보이는 이름" 카드 추가 — 화면을 열면 바로 확인 가능
- 카드를 누르면 계정 설정이 열리고 거기서 이름 변경
- 계정 설정 시트에 이름 편집 행 추가, 설정 화면의 "닉네임" 행도 같은 문구로 정리
- 이름을 한 글자 칠 때마다 iCloud에 업로드하던 문제 수정 — 입력이 멈추고 1초 뒤 한 번만 저장
- 공백만 남기거나 비우면 기본 이름으로 되돌리고, 20자로 제한

**곧 다가올 끼니 자리 미리 잡기 (신규)**
- 식사 시각 1시간 전부터 순간 피드에 점선 셀이 뜹니다. 기록됐을 때 셀이 생길 바로 그 자리(시간순 위치)입니다
- 탭하면 그 끼니로 바로 카메라가 열립니다
- 식사 시각 2시간 뒤까지 기록하지 않으면 사라집니다. 과거의 미기록이 빈 칸으로 쌓이지 않게 하기 위함입니다
- 설정한 아침, 점심, 저녁 알림 시각을 그대로 따르며, 한 번에 하나만 (가장 이른 끼니) 표시합니다
- 앱을 켜둔 채로도 1분마다 시각을 다시 확인해 알아서 나타나고 사라집니다
- 날짜 헤더의 개수는 실제 기록만 셉니다. 운동 모드는 정해진 시각이 없어 제외됩니다

**친구 화면: 사진과 시각**
- 타임라인 카드가 사진 위아래를 잘라내던 것을 원본 비율 그대로 보여주도록 변경 (`scaledToFill` 고정 높이 → `scaledToFit`)
- 식사 시각이 항상 "오전 12:00"으로 나오던 문제 수정. 원인은 업로드 시각을 올리고 받는 쪽에서는 읽지 않아, 날짜(자정)를 그대로 표시하던 것이었습니다
- 이제 **실제로 기록한 시각**을 올리고 내려받아 표시합니다. 친구 화면과 그룹 피드 모두 적용
- 끼니 이름이 길어져도 시각이 밀려 잘리지 않도록 고정 폭 지정, 메모도 길면 접히지 않고 전부 보입니다
- 이미 올라간 예전 기록은 친구 앱이 다시 업로드할 때 정확해집니다

**친구 화면: 응원 남기기**
- 응원(피드백) 버튼이 그리드 모드에만 있어 기본 화면인 타임라인에서는 보이지 않던 문제 해결
- 타임라인의 식단 카드마다 "응원 남기기" 버튼 추가
- 기록이 없는 날에도 "콕 찌르기 · 응원 남기기"로 아침, 점심, 저녁 중 골라 보낼 수 있습니다
- 그룹 피드는 기존대로 읽기 전용을 유지합니다

**운동 기록: 시간대 반영**
- 운동을 항상 아침 자리에 저장하던 것을 **기록한 시각에 맞는 자리**로 변경. 저녁 운동은 저녁 자리에 들어갑니다
- 같은 날 여러 번 기록할 수 있습니다. 이전에는 하루 한 번만 가능했고 두 번째가 첫 번째를 덮어썼습니다
- 격자 화면은 그날의 운동 기록을 시간순으로 모두 보여주고, 새로 기록할 칸 하나를 함께 둡니다
- 화면 표기를 끼니 이름 대신 "운동"과 달리기 아이콘으로 통일 (피드, 상세, 격자)
- 연속 기록, 통계, "거른 날" 판정을 특정 자리가 아니라 "그날 기록이 있는지"로 변경. 저녁 운동이 통계에서 빠지던 문제 해결
- 지난 날에 이미 운동 기록이 있으면 빈 칸을 덧붙이지 않습니다

**상태 문구를 분명하게**
- "기록 필요" → 무엇이 비었는지 그대로 표시: **"식후 사진 기록 필요"**, **"식전 사진 기록 필요"**
- 사진을 넣었는데도 기록이 필요하다고 나와 헷갈리던 문제 해결

**그룹 만들기/참여 화면**
- 다른 곳을 눌러도 키보드가 내려가지 않던 문제 수정 — 빈 곳 탭, 아래로 스크롤, 키보드 위 완료 버튼, 리턴 키 모두로 닫힘
- 만들기/참여 버튼을 누르는 순간에도 키보드가 내려감
- 입력 영역이 스크롤되어 키보드가 버튼을 가리지 않음

**저장 실패 안내**
- iCloud 저장소 준비가 안 되어 실패했을 때 영어 원문 대신 "iCloud 저장소 준비가 아직 끝나지 않았어요. 잠시 후 다시 시도해주세요."로 안내 (피드백, 콕 찌르기, 그룹 만들기 공통)
- 그룹 공개 범위 설정이 서버에 아직 반영되지 않은 경우, "먹은 것까지" 그룹은 정상 생성되도록 처리
- 단, "기록 여부만"을 고른 경우에는 의도보다 많이 공개되지 않도록 그룹을 만들지 않고 안내

**문서**
- 이용약관 페이지 추가 (자동갱신, 해지 방법, 환불 절차, 코인 성격, AI 분석 안내)

## 1.0.6 (2026.08.20)

### 앱스토어 "새로운 기능" (짧은 버전)

```
위젯과 친구 기능이 크게 바뀌었어요.

홈 화면과 잠금화면 위젯이 추가됐어요. 오늘 기록 수와 지금 먹는 중인 끼니를 바로 볼 수 있어요
위젯의 다 먹음 버튼을 한 번 누르면 앱을 열지 않아도 식사가 마무리됩니다
친구는 이제 요청을 보내고 상대가 수락해야 연결돼요. 수락하면 서로의 기록을 볼 수 있어요
그룹이 생겼어요. 초대 코드 하나로 여러 명이 모여 서로의 하루를 함께 봅니다
그룹은 두 가지 방식 중에 고를 수 있어요. 기록 여부만 공유하거나, 사진까지 공유하거나
친구 기록에서 기록이 없는 날은 보이지 않고, 기록이 있는 날만 이어서 보여줘요
친구 사진을 기기에 저장해두어 다시 열 때 훨씬 빠르고 데이터도 덜 씁니다
친구 요청과 그룹 만들기, 참여가 지금 어떤 상태인지 화면에 분명히 보여줍니다
친구 코드를 추가할 때 안 되는 이유를 알려주고, 내 코드가 검색되지 않으면 미리 알려줘요
설정에 예전 기록 공유가 생겼어요. 폰에만 있던 지난 기록을 친구도 볼 수 있게 올릴 수 있어요
```

### 상세 변경 내역

**홈/잠금화면 위젯 (신규)**
- SekkiWidget 위젯 익스텐션 탑재: 홈 화면(systemSmall) + 잠금화면(사각형/원형/인라인)
- 표시 내용: 오늘 기록 수, 지금 "먹는 중"인 끼니, 마지막 기록 시각, "기록 필요" 수
- 위젯의 "다 먹음" 버튼: "먹는 중"인 기록이 있으면 그 기록을 마감하고, 없으면 시각으로 추론한 끼니에 새 기록 적재 (앱 실행 불필요)
- 앱은 기록 저장, 로드 시 App Group에 경량 스냅샷만 기록해 위젯을 갱신 (사진 전체를 디코딩하지 않아 가볍고 빠름)
- 자정이 지나면 "오늘 N개"가 저절로 리셋되도록 타임라인 갱신 시점 조정

**친구 요청과 수락 (양방향)**
- 코드를 넣으면 곧바로 추가되던 방식에서, **요청을 보내고 상대가 수락**해야 연결되는 방식으로 변경
- 화면에 "받은 친구 요청"(수락/거절)과 "보낸 요청"(수락 대기 중 / 거절됨, 요청 취소) 섹션 추가
- 상대가 이미 나에게 요청을 보내둔 상태에서 내가 요청하면 즉시 성사
- 요청, 수락 시 상대에게 푸시 알림 (CloudKit 구독)
- 기존 친구는 그대로 유지, 앱 첫 실행 때 자동 이관되고, 상대가 아직 업데이트하지 않아도 친구로 인정
- 친구 끊기는 내 쪽 연결만 정리

**그룹 (신규)**
- 그룹을 만들면 6자리 초대 코드 발급, 코드를 받은 사람은 누구나 참여
- 그룹 화면에서 **멤버 전원의 하루 기록**을 한 화면에서 날짜별로 열람 (이전, 다음 버튼으로 날짜 이동)
- 멤버별 기록은 (사람과 끼니 조합) 레코드를 100개씩 묶어 한 번에 받아오고 기존 친구 캐시를 공유, 인원이 늘어도 왕복이 늘지 않음
- 방장이 나가면 그룹이 삭제됨. 강퇴는 제공하지 않음(공개 DB에서는 남의 레코드를 지울 수 없음)
- 주의: 수락, 그룹 가입은 앱 화면에 무엇을 보여줄지를 정하는 장치이며, 공개 DB 특성상 읽기 자체를 차단하지는 않습니다

**친구 기록 보기: 빈 날 숨김**
- 친구 타임라인에서 기록 없는 날의 "기록 없음" 칸을 없애고, **기록이 있는 날만** 이어서 보여줍니다
- 빈 구간은 자동으로 건너뛰며 과거로 확장(최대 1년), 바닥에 "이전 기록 더 보기" / "더 이상 기록이 없어요" 표시
- 하루씩 왕복하던 조회를 **날짜 묶음 배치 조회**로 변경 (14일치를 한 번에), 기록 없는 날도 결과를 기억해 같은 날을 다시 요청하지 않음
- 그룹 피드도 동일: 그 날 기록이 없는 멤버는 숨기고, 아무도 없으면 "이 날은 아무도 기록하지 않았어요"

**친구 사진 로컬 캐시 (같은 사진 다시 안 받기)**
- 친구와 그룹 식단 캐시를 시스템이 비울 수 있는 Caches에서 **Application Support의 영구 저장소**로 이전 (기존 캐시는 그대로 이관, iCloud 백업에서는 제외)
- 지난 날짜는 기록이 바뀌지 않으므로 **만료 없이 계속 재사용** (기존에는 7일마다 사진을 통째로 다시 다운로드)
- 기록이 없는 날도 "없음"으로 캐시, 빈 날을 지나칠 때마다 다시 조회하던 문제 해결
- 메모리 캐시를 개수(200) + 용량(80MB) 양쪽으로 제한, 디스크는 300MB 상한을 넘으면 오래된 것부터 정리
- 그리드 모드도 30일치를 한 번에 조회 (하루씩 30번 왕복하지 않음)
- 친구 타임라인에 당겨서 새로고침 추가, 친구가 예전 기록을 고쳤을 때 갱신하는 경로

**예전 기록 공유 (백필) + 업로드 대상 판정 수정**
- 지금까지 업로드는 저장 시점 기준 **최근 3일만** 대상으로 삼았습니다. 그래서 오래된 기록을 고쳐도 서버에 반영되지 않았고, 앱을 쓰기 전, 예전 버전의 기록은 아예 올라간 적이 없었습니다
- 이제 **실제로 내용이 바뀐 날짜**만 정확히 골라 올립니다 (날짜별 지문 비교). 업데이트 직후 이력 전체가 한꺼번에 올라가지 않도록 첫 실행 때 기준값을 잡습니다
- 그 날 기록을 모두 지우면 **서버에서도 삭제**됩니다 (예전에는 친구 화면에 계속 남았습니다)
- 설정의 계정과 공유 항목에 있는 **예전 기록 공유**: 폰에만 있는 지난 기록을 직접 올릴 수 있습니다
  - 범위 선택(최근 3개월 / 1년 / 전체), 올릴 일수, 사진 장수, 예상 용량 표시
  - 이미 올라간 날짜는 사진을 받지 않고 존재만 확인해 건너뜁니다
  - 중단해도 다음에 이어서 진행, 실패한 날짜만 재시도
- 운동 모드에서 저장할 때 운동 기록이 식단으로 업로드될 수 있던 경로도 함께 차단

**그룹 공개 범위 (방장이 선택)**
- 그룹을 만들 때 두 가지 중에 고르고, 방장은 언제든 바꿀 수 있습니다
  - **기록 여부만**: 누가 어떤 끼니를 기록했는지만 보여줍니다. 사진과 메모는 서로 보이지 않아요. 멤버 전원을 기록함/안 함으로 표시해 누가 빠졌는지 한눈에 보입니다
  - **먹은 것까지**: 지금까지처럼 사진과 메모를 그대로 보여줍니다
- 기록 여부만 공유하는 그룹은 **사진을 아예 내려받지 않습니다** (필요한 정보만 조회), 빠르고 데이터도 적게 씁니다
- 그룹 목록과 피드 상단에 현재 범위를 표시하고, 방장이 아니면 바꿀 수 없습니다 (서버에서도 방장만 수정 가능)
- 예전에 만든 그룹은 기존처럼 사진까지 공유하는 상태로 유지됩니다

**진행 상태 표시 (요청, 그룹)**
- 친구 요청, 그룹 만들기, 그룹 참여가 **시트 안에서 끝까지** 진행됩니다. 보내는 중에는 진행 표시와 문구가 뜨고, 성공/실패 결과도 같은 화면에서 확인합니다
  (이전에는 시트를 닫은 뒤 알럿을 띄우다 보니, 닫히는 도중 알럿이 묻히면 아무 반응 없이 끝난 것처럼 보여 두 번 누르게 됐습니다)
- 그룹을 만들면 **초대 코드를 바로 보여주고 복사**할 수 있습니다
- 처리 중에는 버튼, 입력, 닫기가 잠겨 중복 실행이 되지 않습니다
- 목록이 **불러오는 중인지, 실패했는지, 정말 비어 있는지**를 구분해서 표시하고, 실패 시 사유와 다시 시도 버튼을 제공합니다 (친구 목록, 그룹 목록 공통)
- 요청 수락, 거절 버튼도 진행 표시와 실패 사유를 그 자리에 보여줍니다
- 그룹 나가기 중에는 진행 중 화면을 표시합니다

**친구 코드 오류 진단**
- "존재하지 않는 코드입니다" 안내에 확인 방법 추가: 친구 앱에 지금 표시된 코드가 맞는지, 서로 같은 경로(TestFlight/앱스토어)로 설치했는지
- CloudKit 조회 실패를 원인별로 구분해 안내 (스키마 미준비 / 검색 인덱스 없음 / 네트워크 / iCloud 미로그인), 이전엔 모두 원문 오류로 노출
- **내 코드 자가검증**: 친구 화면 진입 시 내 코드가 실제로 서버에서 검색되는지 확인하고, 검색되지 않으면 코드 아래에 주황색 경고 + "다시 확인" 버튼 표시
  (화면의 코드는 로컬 캐시에서도 나오기 때문에, 레코드가 없거나 배포 환경이 다른 "유령 코드" 상태를 사용자가 바로 알 수 있음)
- 갓 만든 레코드의 인덱싱 지연으로 인한 오탐을 막기 위해 3초 간격 3회 확인 후에만 경고

**내부**
- LeeoKit 의존성을 로컬 경로에서 원격 2.7.0 핀으로 고정
- WidgetSnapshot/WidgetStateResolver를 RoutineCameraCore로 이관(중복 제거), 위젯 관련 테스트 9개 추가 (전체 33개 통과)
- FriendLink, RCGroup, GroupMember 레코드 타입 추가. **CloudKit Console에서 Production 스키마와 인덱스 배포가 필요합니다**
- 빌드 번호 2

## 1.0.5 (2026.07.26)

### 앱스토어 "새로운 기능" (짧은 버전)

```
빈 칸 없는 기록으로 새로 태어났어요.

삼시세끼 칸을 채우는 방식을 없앴어요. 남긴 기록만 사진 일기처럼 시간순으로 쌓입니다
끼니는 촬영 시각으로 알아서 인식해요. 아침, 점심, 저녁을 고를 필요 없어요
사진을 못 찍었어도 괜찮아요. 사진 없이 기록 버튼 한 번이면 끝납니다
식전 사진만 찍었다면 알림에서 다 먹음을 한 번 눌러 마무리할 수 있어요. 앱을 열지 않아도 됩니다
액션 버튼과 시리, 단축어로도 다 먹음을 남길 수 있어요
챙기고 싶은 식사만 골라 알림을 받아요. 세 끼 전부 알림받는 부담이 없습니다
한 끼만 기록해도 연속 기록이 이어져요. 기록한 날은 줄어들지 않습니다
로그인이 사라졌어요. iCloud 계정으로 친구 기능이 바로 동작합니다
화면 이동이 iOS 기본 방식으로 자연스러워지고, 기록 버튼은 항상 손 닿는 하단에 있습니다
```

### 상세 변경 내역

**메인 UX: 순간 컬렉션 피드**
- 3끼 격자(빈 슬롯)를 기본에서 내리고, 남긴 기록만 시간순으로 쌓이는 사진 일기 피드로 전환 (기존 격자는 설정 "화면 방식"에서 계속 사용 가능)
- 끼니 자동 추론: 촬영 시각으로 아침/점심/저녁/간식 판별 (5~10 아침, 11~15 점심, 16~21 저녁, 그 외 간식). 기록 버튼에 "점심 기록하기"처럼 무엇으로 인식될지 미리 표시
- 같은 날 안에서도 시간 흐름대로 정렬 (타임스탬프 없는 과거 기록은 끼니 대표 시간으로 보정)
- 피드를 List로 전환, 셀 스와이프 삭제, 상세는 모달 대신 네이티브 push
- 상태 태그를 하나만 배타적으로 표시: `식사 완료`(식전+식후) / `다 먹음` / `먹는 중`(오늘) / `기록 필요`(지난 날)
  - "먹는 중"은 당일에만 표시되고, 날이 지나면 "기록 필요"로 바뀜
  - 기존 "식전, 식후 비교" 라벨은 "식사 완료"로 변경
- 운동 모드도 같은 피드 경험으로: 라벨 "운동"(초록), 음식 태그 대신 "완료" 표시, 빈 상태, 버튼 문구도 모드별. 설정에 "운동 모드로 기록" 토글 추가

**앱을 열지 않는 기록**
- 알림 액션 "다 먹음": 식전 사진만 남기면 잠시 후 "다 드셨어요?" 알림이 오고, 배너에서 원탭으로 마무리
- App Intents: 액션 버튼, 시리, 단축어로 "다 먹음" 기록
- 원탭 다먹음과 리마인더는 **당일 기록**에만 적용 (지난 날 미마감 기록이 뒤늦게 마감되지 않음)
- App Group 배관 + 위젯 코드 준비 (위젯 익스텐션 타깃은 아직 미연결, 이번 버전에서 위젯은 노출되지 않음)

**기록 부담 줄이기**
- "사진 없이 기록"을 토글+완료 2단계에서 한 탭 버튼으로 교체
- "챙길 식사"만 알림: 신경 쓰고 싶은 식사만 골라 그 끼니만 리마인드 (아무것도 안 골라도 OK). 첫 실행 시 선택 프롬프트, 설정에서 언제든 변경
- 하루 완료 기준 완화: "주요 3끼 전부"에서 "한 끼라도 기록"으로 바뀌어 연속 기록 유지
- 통계의 "최고 연속"을 "기록한 날"(줄어들지 않는 누적)로 교체, 목표 진행률 바도 누적 기준으로
- 끊긴 날의 빨간 배경/테두리 제거, 문구도 "다음 기록부터 다시 이어져요"로

**계정: Firebase 제거, CloudKit 전환**
- 친구 기능이 iCloud 계정 기반으로 동작하면서 **Apple 로그인 화면 자체가 사라짐**
- 친구 식단 사진을 CKAsset으로 주고받아 로딩 개선, 피드백은 레코드 하나로 저장+알림 통합
- 읽음 상태는 수신 기기에 로컬 저장. 이전 Firebase 데이터는 이관하지 않음
- 구독, 코인 결제를 LeeoKit(LeeoStore, LeeoConsumableStore)로 이관

**화면 구조, 설정**
- 커스텀 헤더를 네이티브 NavigationStack으로 교체: 날짜/모드는 navigationTitle, 모드 전환, 통계, 친구, 설정은 toolbar, 목표 진행률은 상단 얇은 바. 상세로 들어갈 때 뒤로가기와 타이틀 전환이 정상 동작
- 기록 버튼을 하단 고정으로 이동 (스크롤해도 항상 보임)
- 설정 17개 섹션을 위계순으로 재정렬(화면, 표시, 챙길 식사, 알림, 목표, 사진 저장, 계정과 공유, 고급, 정보, 지원 순), AI 분석은 하위 페이지로 분리, 개발용 항목은 맨 아래로

**품질**
- 빌드 경고 17건에서 0건으로 (deprecated API 교체, 동시성 격리 경고 정리)
- 핵심 로직을 RoutineCameraCore 로컬 패키지로 분리하고 단위 테스트 24개 추가 (끼니 추론, 스트릭, 통계, 정렬, Codable 구버전 호환, 먹는 중/기록 필요 판정), 전부 통과
- ContentView.swift 모놀리스 분리 (3232줄에서 429줄로, 7개 파일)
- 헤더 날짜 잘림, 챙길 식사 시트 내용 잘림 등 레이아웃 수정

### 릴리스 전 확인
- [ ] Xcode에서 빌드 번호(CURRENT_PROJECT_VERSION) 올리기
- [ ] 위젯을 이번 버전에 포함할지 결정 (포함하려면 SekkiWidget 타깃 생성과 App Group 연결이 필요, SekkiWidget/README.md 참고)
- [ ] 기존 사용자 데이터 마이그레이션 확인 (표준 저장소에서 App Group 공유 저장소로 1회 이관, capturedAt 없는 과거 기록 정렬)
- [ ] CloudKit 스키마 Production 배포 확인 (CLOUDKIT_SETUP.md)
- [ ] 알림 액션 "다 먹음" 실기기 확인 (앱 미실행 상태에서 원탭 후 앱 활성화 시 반영)

## 1.0.4 (2026.07)

### 앱스토어 "새로운 기능" (짧은 버전)

```
더 쉬운 기록, 더 가까운 친구

홈 화면이 새로워졌어요. 더 심플하고 한눈에 들어오는 디자인입니다
식사 시간이 지나면 기록할 시간 배너가 떠요. 탭 한 번이면 바로 카메라로 넘어갑니다
과거 기록을 보다가 오늘 버튼으로 바로 돌아올 수 있어요
콕 찌르기가 생겼어요. 친구가 아직 식사를 기록하지 않았다면 콕 찔러 보세요
기록 전 끼니에 온 친구의 메시지도 배지로 확인할 수 있어요
친구가 피드백을 남기면 푸시로 알려드려요
친구 코드 여러 개를 한 번에 붙여넣어 추가할 수 있어요
친구 식단 사진이 훨씬 빨리 열려요
카메라 화면 정리와 자잘한 사용성 개선
```

### 상세 변경 내역

**기록 경험**
- 앱 진입 시 카메라가 강제로 열리던 동작을 제거하고 홈 상단 "기록할 시간이에요" 배너로 교체 (탭하면 카메라, 기록하면 다음 미기록 끼니로 자동 갱신). 설정 문구는 "기록 시간 배너 표시"로 변경
- 과거 날짜를 보는 중이면 우하단에 "오늘" 플로팅 버튼 표시
- 끼니 칸이 4개 이상이면 4번째 칸이 살짝 보이도록(peek) 크기 조정, 옆으로 더 있음을 암시
- 과거의 빈 끼니 칸에도 + 아이콘 표시 (탭하면 소급 기록)
- 빈 간식 칸이 "간식 보이기" 설정을 따르도록 수정
- 카메라 화면 정리: 중복 취소 버튼 제거, 셔터 중앙 정렬(표준 스타일), 정사각 촬영 프레임 경계 표시, 미리보기 버튼 정돈

**디자인**
- 홈 상단 두 헤더(아이콘 줄 + 날짜 줄)를 한 줄로 통합: 날짜, 오늘 뱃지(탭=오늘로 이동), 모드 전환, 통계/친구/설정
- 미니멀 리디자인: 그라디언트/그림자/장식 제거, 액센트는 시스템 블루 하나, 끼니 심볼 색만 유지
- 목표 진행률을 얇은 바 + "n/N일" 한 줄로 압축 (달성 시 초록)
- 라이트/다크 모드 모두 점검

**친구 & 피드백 (이벤트 대비)**
- 콕 찌르기: 친구의 빈 끼니 칸을 탭하면 콕 찌르기 또는 댓글 남기기
- 수신 확장: 내 빈 끼니 칸에도 안 읽은 피드백 배지 표시, 탭하면 목록 열람(자동 읽음 처리) + "지금 기록하기" 버튼
- 피드백 푸시 알림: CloudKit 기반 (서버 불필요, 내용은 전송하지 않고 알림 신호만; 보낸 신호는 7일 후 자동 정리)
- 친구 코드 일괄 추가: 쉼표/공백/줄바꿈으로 구분해 여러 코드 붙여넣기 지원
- 친구 식단 사진 업로드를 자동 축소(긴 변 1024px)해 로딩 속도, 데이터 사용량 대폭 개선

**품질/접근성**
- 홈 스크롤 중 헤더가 튀던 커스텀 제스처 제거
- 화면을 볼 때마다 반복되던 불필요한 연산 제거 (과거로 깊이 스크롤할수록 무거워지던 문제)
- 앱 사용 중에도 알림이 배너로 표시되도록 개선
- 새 상태들에 대한 VoiceOver 안내 추가 (배너, 피드백 배지, 콕 찌르기 등)

### 릴리스 전 확인
- [ ] 실기기 2대로 콕 찌르기 후 푸시 수신 검증 (미검증 시 앱스토어 문구에서 푸시 줄 제외 권장)
- [ ] Xcode Signing & Capabilities: iCloud(CloudKit) 컨테이너 + Push Notifications 체크
- [ ] TestFlight/앱스토어 배포 전 CloudKit 스키마 Production 배포 (FIREBASE_SETUP.md)
